#pragma once
#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <ctime>

using namespace std;

struct employee;

void input_file(string);
void output_file(string);
vector<string> split(string, char);